export type User = {
  id: string;
  password: string;
  firstName: string;
  lastName: string;
  email: string;
};

export type ReturnUser = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
};
